modded class WeaponChambering_Base
{
	//! Suppress crash.log debug spam by only printing to script log
	void Error(string err)
	{
		Print(err);
	}
}

modded class WeaponChambering
{
	//! Suppress crash.log debug spam by only printing to script log
	void Error(string err)
	{
		Print(err);
	}
}
